<h1>Список лікарень</h1>

<% if @hospitals.any? %>
  <table>
    <thead>
      <tr>
        <th>Рейтинговий номер (id)</th>
        <th>Назва</th>
        <th>Посилання</th>
        <th>Країна</th>
        <th>Місто</th>
        <th colspan="5"></th>
      </tr>
    </thead>
    <tbody>
      <% @hospitals.each do |hospital| %>
        <tr>
          <td><%= hospital.rank %></td>
          <td><%= hospital.name %></td>
          <td><%= hospital.url %></td>
          <td><%= hospital.country %></td>
          <td><%= hospital.city %></td>
          <td><%= link_to 'Перегляд', hospital %></td>
          <td><%= link_to 'Редагувати', edit_hospital_path(hospital) %></td>
          <td><%= link_to 'Видалити', hospital, method: :delete, data: { confirm: 'Ви впевнені?' } %></td>
        </tr>
      <!-- Can remove next code? -->
        <% if hospital.doctors.any? %>
          <tr>
            <td colspan="6">
              <h3>Лікарі в цій лікарні:</h3>
              <% hospital.doctors.each do |doctor| %>
                <p>
                  <%= link_to doctor.dname, doctor %>
                </p>
              <% end %>
            </td>
          </tr>
        <% end %>
      <% end %>
    </tbody>
  </table>
<% else %>
  <p>Немає жодної лікарні.</p>
<% end %>

<br>
<%= link_to 'Створити нову лікарню', new_hospital_path %>
<br>
<%= link_to 'Повернутися на головну сторінку', root_path %>